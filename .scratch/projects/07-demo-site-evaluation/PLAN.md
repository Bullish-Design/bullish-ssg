# PLAN

NO SUBAGENTS: Execute all tasks directly in this agent.

1. Establish project hygiene files (`CONTEXT`, `PROGRESS`, `DECISIONS`, `ISSUES`) and baseline assumptions.
2. Finalize demo site information architecture for publishable docs/blog content (`healthy`) and test-only negative fixtures (`broken`).
3. Create a step-by-step implementation runbook for building demo content and verification workflows.
4. Define a strict command verification matrix for all 7 CLI commands, with expected outputs and exit codes.
5. Define publish workflow for GitHub Pages dry-run and live deploy.
6. Define acceptance criteria and handoff checklist with evidence capture requirements.

NO SUBAGENTS: Keep all implementation and planning local.
