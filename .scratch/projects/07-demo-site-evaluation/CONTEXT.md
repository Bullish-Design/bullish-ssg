# CONTEXT

This project is planning-only right now. No demo content has been implemented yet.

Current state:
- `ASSUMPTIONS.md` and `PLAN.md` existed.
- Required template files were missing (`PROGRESS`, `DECISIONS`, `ISSUES`, `CONTEXT`).
- A new implementation runbook is needed to move from concept to execution.

Immediate next action:
- Produce a detailed demo-site runbook with phased implementation steps, test checkpoints after each step, and a command matrix.
